bugfixes:
  - "The ``--collection-dir`` option was added to the wrong CLI argument parser.
     This had no practical effect for end-users, but would also add it to other build-data based subcommands added in the future
     (https://github.com/ansible-community/antsibull/pull/461)."
